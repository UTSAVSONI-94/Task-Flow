{
  "name": "taskflow",
  "version": "1.0.0",
  "private": true,
  "description": "TaskFlow Monorepo Root",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "install": "cd server && npm install",
    "build": "cd server && npm run build",
    "start": "cd server && npm start"
  }
}
